[@nimiq/core](../globals.md) / PlainSignalDataUpdateMode

# Type Alias: PlainSignalDataUpdateMode

> **PlainSignalDataUpdateMode** = `"full"` \| `"version"`

Defined in: @nimiq/core/types/wasm/web.d.ts:582

Whether a set signal data (warm-key) transaction replaces the entire `signalData` field or
only updates the protocol-version bytes (preserving the rest).
